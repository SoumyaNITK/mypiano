<div class="post-card" data-section="{{ post.data.section }}">
  <a href="{{ post.url }}">
    <h3>{{ post.data.title }}</h3>
    <p class="date">{{ post.date | date("yyyy-MM-dd") }}</p>
    <p class="section">{{ post.data.section | capitalize }}</p>
  </a>
</div>
